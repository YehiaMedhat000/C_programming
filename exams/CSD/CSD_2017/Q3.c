#include <stdio.h>

/**
 * main - Entry point
 * @ac: Arguments count
 * @av: Arguments array
 * Return: 0 in success, -1 otherwise
 */

int main(int ac, char **av)
{
	/* Place holder for now */
	return (0);
}
